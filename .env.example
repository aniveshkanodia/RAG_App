# RAG Application Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Supabase Configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# OPTIONAL BACKEND VARIABLES (with defaults)
# =============================================================================

# API Server Configuration
# API_HOST=0.0.0.0                    # Backend server host (default: 0.0.0.0)
# API_PORT=8000                       # Backend server port (default: 8000)
# API_RELOAD=true                      # Enable auto-reload for development (default: true)

# CORS Configuration
# CORS_ORIGINS=http://localhost:3000  # Comma-separated list of allowed CORS origins (default: *)
# CORS_CREDENTIALS=false              # Enable CORS credentials (default: false)

# Logging Configuration
# RAG_LOG_PATH=logs/rag_turns.jsonl    # Path to RAG turn logs (default: logs/rag_turns.jsonl)

# LangSmith Tracing (Optional - for debugging/monitoring)
# Enable LangSmith tracing to monitor LLM calls
# LANGCHAIN_TRACING_V2=false           # Enable LangSmith tracing (default: false)
# LANGCHAIN_ENDPOINT=                  # LangSmith endpoint URL (default: empty)
# LANGCHAIN_API_KEY=                   # LangSmith API key (default: empty)
# LANGCHAIN_PROJECT=                   # LangSmith project name (default: empty)

# =============================================================================
# OPTIONAL FRONTEND VARIABLES
# =============================================================================

# Frontend API Configuration
# Create a .env.local file in the frontend/ directory with:
# NEXT_PUBLIC_API_URL=http://localhost:8000
# 
# Or set it when running: NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
